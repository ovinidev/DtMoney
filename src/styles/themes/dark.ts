import { createTheme } from "../stitches.config";

export const dark = createTheme({
  colors: {
    primary: '',
    secondary: '',
    tertiary: '',
    quaternary: '',
    black: '',
    white: '',
  },
})