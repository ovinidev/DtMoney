import { createTheme } from "../stitches.config";

export const light = createTheme({
  colors: {
    primary: 'blue',
    secondary: '',
    tertiary: '',
    quaternary: '',
    black: '',
    white: '',
  },
})